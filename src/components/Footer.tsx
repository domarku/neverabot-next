export default function Footer() {
  return (
    <footer>
      <p>
        © {new Date().getFullYear()} — neverabot.org is maintained by Dominik
        Markušić Gross
      </p>
    </footer>
  );
}
